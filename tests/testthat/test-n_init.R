# starting pop
map(n_init, colMeans)

map(n_init, head)

# c_icd = 1
head(res[[8]]$c_total)
colMeans(res[[8]]$c_total)

n_init_fixed <- c(2569, 3134, 3113)
n_total - n_init_fixed

abline(h = (n_total - n_init_fixed)- 559)
